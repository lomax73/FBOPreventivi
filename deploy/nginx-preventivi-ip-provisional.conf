# Config REALMENTE in uso in produzione finché non esiste un dominio:
# MKRemote è su 94.177.161.127:443, il Portale su :8443, Collaudi Fibra
# su :8444 (tutti self-signed), questa app usa la porta 8445 sullo
# stesso IP.
# Da sostituire con nginx-preventivi.conf (dominio + Let's Encrypt) appena
# disponibile un dominio reale — vedi deploy/README.md.
server {
    listen 8445 ssl;
    server_name 94.177.161.127;

    ssl_certificate     /etc/ssl/preventivi/selfsigned.crt;
    ssl_certificate_key /etc/ssl/preventivi/selfsigned.key;

    client_max_body_size 10M;

    location /api/internal/ {
        allow 127.0.0.1;
        deny all;
        proxy_pass http://unix:/run/preventivi/gunicorn.sock;
        proxy_set_header Host $http_host;
        proxy_set_header X-Forwarded-Proto https;
    }

    location /static/ {
        alias /opt/preventivi/app/staticfiles/;
    }

    location /media/ {
        alias /opt/preventivi/app/media/;
    }

    location / {
        proxy_pass http://unix:/run/preventivi/gunicorn.sock;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}
